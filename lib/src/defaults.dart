import 'package:flutter/widgets.dart';

/// Default animation duration milliseconds is 300.
const animationDuration = Duration(milliseconds: 300);

/// Default padding all 8.0.
const paddingAll8 = EdgeInsets.all(8.0);

/// Default padding horizontal is 16.0 and vertical is 8.0.
const paddingH16V8 = EdgeInsets.symmetric(
  horizontal: 16.0,
  vertical: 8.0,
);
